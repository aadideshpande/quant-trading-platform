apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: dev
spec:
  selector:
    app: order-service
  ports:
    - port: 5000       # internal port
      targetPort: 5000 # matches containerPort
      nodePort: 30001  # NodePort for external local access
  type: NodePort
